<?php 

    // $h =  $block->getHeightData();
    // $w = $block->getWeightData();

    // echo $total = (int)$h + (int)$w;
    $array =  array($block->getNumeroData(), $block->getServiceApi());
    print_r(json_encode($array));
?>